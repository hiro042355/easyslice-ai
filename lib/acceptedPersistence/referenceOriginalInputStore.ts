import type { OriginalInputRecordKey,OriginalWorkflowInputRecord } from "./types";
import { copy,validOpaque } from "./acceptedPersistenceUtils";
export class ReferenceOriginalInputStore{private values=new Map<string,{key:OriginalInputRecordKey,value:OriginalWorkflowInputRecord}>();private next=1;async create(identity:string,value:OriginalWorkflowInputRecord){if(!validOpaque(identity)||!value)return{status:"failed" as const};if(this.values.has(identity))return{status:"conflict" as const};const key={keyVersion:"1.0",internalKey:`[reference-original-input-key-${this.next++}]`}as OriginalInputRecordKey;this.values.set(identity,{key:copy(key),value:copy(value)});return{status:"created" as const,key:copy(key)}}async read(key:OriginalInputRecordKey){const e=[...this.values.values()].find(v=>v.key.internalKey===key?.internalKey);return e?{status:"found" as const,key:copy(e.key),value:copy(e.value)}:{status:"missing" as const}}async delete(key:OriginalInputRecordKey){const e=[...this.values.entries()].find(([,v])=>v.key.internalKey===key?.internalKey);if(!e)return{status:"missing" as const};this.values.delete(e[0]);return{status:"deleted" as const}}}
