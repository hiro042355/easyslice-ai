import {buildReferenceMVRequest} from "@/lib/providers/referenceMVAdapter";import type {ReferenceMVWorkflowInput} from "./referenceWorkflowTypes";import type {ReferenceWorkflowResult} from "./types";import type{ReferenceWorkflowExecutionDependencies,ReferenceWorkflowLogicalAssetRequirement}from"@/lib/workflowEntry/types";import {runReferenceGenerationWorkflow} from "./referenceGenerationWorkflow";
export const runReferenceMVWorkflow=(input:ReferenceMVWorkflowInput,dependencies?:ReferenceWorkflowExecutionDependencies):Promise<ReferenceWorkflowResult>=>runReferenceGenerationWorkflow(input,{build:buildReferenceMVRequest,projectAssets:(request,workflowInput)=>{const out:ReferenceWorkflowLogicalAssetRequirement[]=[];const audio=workflowInput.assets.find(value=>value.assetId===request.audioAssetId);if(!audio)return undefined;out.push({assetRef:audio,requirement:"required",usage:"audio-conditioning"});const seen=new Map<string,string>();for(const scene of request.scenes??[])for(const id of scene.assetIds??[]){const asset=workflowInput.assets.find(value=>value.assetId===id);if(!asset)return undefined;const usage=asset.kind==="video"?"reference-video":asset.kind==="character"?"character-identity":asset.kind==="brand"?"location-reference":"reference-image";if(seen.has(id)&&seen.get(id)!==usage)return undefined;seen.set(id,usage);if(!out.some(value=>value.assetRef.assetId===id&&value.usage===usage))out.push({assetRef:asset,requirement:"optional",usage})}return out}},dependencies);
export const createReferenceMVWorkflow=(dependencies?:ReferenceWorkflowExecutionDependencies)=>({run:(input:ReferenceMVWorkflowInput)=>runReferenceMVWorkflow(input,dependencies)});
