import type{ProviderCredentialHandle,ProviderCredentialState}from"@/lib/providerClients/types";import{copy}from"./operationPipelineUtils";const states:Readonly<Record<string,ProviderCredentialState>>=Object.freeze({"credential-valid":"valid","credential-expired":"expired","credential-missing":"missing"});export const referenceCredentialHandle=Object.freeze({credentialRef:"credential-valid",providerId:"reference-provider",credentialVersion:"reference-v1"})as ProviderCredentialHandle;export const resolveReferenceCredential=(ref:string)=>({handle:copy(referenceCredentialHandle),state:states[ref]??"missing",scopeClass:"generation",region:"reference-region",expiresAt:"2030-01-02T00:00:00.000Z"}as const);